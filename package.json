{
  "name": "independentreserve",
  "version": "1.0.0",
  "description": "Wrapper client for Independent Reserve, Australian crypto exchange.",
  "main": "dist/index.js",
  "scripts": {
    "test": "npm run test",
    "watch": "nodemon --watch 'src/**/*.ts' --ignore 'src/**/*.spec.ts' --exec 'ts-node' src/index.ts",
    "build": "ntsc -p ./"
  },
  "author": "Michael Kimpton",
  "license": "WTFPL",
  "dependencies": {
    "request": "^2.83.0",
    "request-promise": "^4.2.2",
    "utf8": "^3.0.0"
  },
  "devDependencies": {
    "@types/node": "^8.5.7",
    "ntypescript": "1.201706190042.1",
    "typescript": "^2.6.2"
  }
}
